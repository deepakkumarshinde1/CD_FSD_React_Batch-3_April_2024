// area of circle
function areaOfCircle(r = 1) {
  const PI = 3.14;
  let result = PI * (r * r); // NaN => not a number
  console.log(`Area of circle for ${r} radius is ${result}`);
}

areaOfCircle(53);

// #1 number even or odd with function
// #2 check if the number is prime number or not
